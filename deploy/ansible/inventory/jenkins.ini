# Committed inventory for Jenkins when the controller runs ON the same VPS as the app.
# Requires: deploy user in docker group; SSH key in Jenkins credential vps-ssh-deploy-key.
# For a remote VPS, use hosts.ini (gitignored) instead and point ansible_host at that IP.

[vps]
localhost ansible_host=127.0.0.1 ansible_user=deploy
